# Open Graph Image

The default Open Graph image is `public/og-image.svg`.

- Size: 1200 x 630
- Used by: `src/layouts/Layout.astro`
- Fallback for blog posts without a dedicated image: `src/pages/blog/[slug].astro`

Update `og-image.svg` whenever the site's positioning or primary visual identity changes.
